Rob's Focuser Driver INSTALL
============================
1) $ mkdir bld
2) $ cd bld
3) $ cmake -DCMAKE_INSTALL_PREFIX=/usr . ..
4) $ sudo make install
   
Refer to README for instructions on using the driver.

CMake compilation options: none.
